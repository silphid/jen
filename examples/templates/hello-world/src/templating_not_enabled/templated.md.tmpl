# Template rendering activated

Because this file has the `.tmpl` extension, template rendering gets activated in it. Therefore this {{.TEAM}} expression gets interpolated. The `.tmpl` extension also gets trimmed away from the final file name.